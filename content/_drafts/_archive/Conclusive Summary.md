In conclusion, the hyperparameter analysis presented in Table 1 highlights distinct characteristics of the support vector regression (SVR) models with different kernels. The \verb|lin-SVR| and \verb|rbf-SVR| models demonstrate considerable sensitivity to outliers, as indicated by their large regularization parameter values. Moreover, the linear and rbf SVR models exhibit higher complexity compared to the polynomial SVR model, reflected in their smaller $\varepsilon$ values. The tradeoff between accuracy and model complexity is evident, with the \verb|lin-SVR| model offering precise predictions but potentially prone to overfitting, while the \verb|poly-SVR| model prioritizes general trends with a larger error tolerance. These findings provide insights for selecting appropriate hyperparameters based on the prediction objectives and dataset characteristics.

  
In conclusion, the performance evaluation of the three optimized Support Vector Regression (SVR) models provides valuable insights. The linear-kernel SVR model demonstrates the highest performance with the lowest test Root Mean Square Error (RMSE) and Mean Absolute Percentage Error (MAPE), indicating its superior predictive accuracy compared to the other models. The rbf-kernel SVR model follows closely, while the polynomial-kernel SVR model exhibits the lowest level of performance. Additionally, the linear-kernel and rbf-kernel SVR models exhibit favorable accuracy metrics, as reflected by their low RMSE values, while the polynomial-kernel model shows a significantly larger prediction error. The high coefficient of determination ($R^2$) scores for linear-kernel and rbf-kernel SVR models further validate their alignment with the actual stock prices. However, it's worth noting that the linear-kernel SVR model's excessive utilization of support vectors raises concerns about overfitting, highlighting the need for careful consideration when applying these models to unseen data.

---
See also: [[Thesis Scratchpad]]