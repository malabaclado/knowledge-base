---
tags: w3schools type/flashcard-note 
cards-deck: Python
---
# Sets

The {intersection_update()} set method removes the items that is not present in both sets (or in all sets if the comparison is done between more than two sets). #set-methods
^1680171178619

The intersection_update() method is different from the intersection() method, because {the intersection() method returns a new set, without the unwanted items, and the intersection_update() method removes the unwanted items from the original set.} #set-methods
^1680171210236

The {isdisjoint()} set method returns True if none of the items are present in both sets, otherwise it returns False. #set-methods
^1680171410235

How to check if two sets have an intersection?:: *set.isdisjoint(set)* #set-methods
^1680171410238

The {issubset()} set method returns True if all items in the set exists in the specified set, otherwise it returns False. #set-methods
^1680171716767

The {pop()} set method removes a random item from the set. This method returns the removed item. #set-methods
^1680171716770

The remove() method is different from the discard() method, because {the remove() method will raise an error if the specified item does not exist, and the discard() method will not.} #set-methods
^1680171716773

The {symmetric_difference()} set method returns a set that contains all items from both set, but not the items that are present in both sets. #set-methods
^1680171716776

The {`update()`} method updates the current set, by adding items from another set (or any other iterable). #set-methods
^1680172003593

How to add multiple elements to a set from another set (or any iterable?)?:: `set.update(set)` #set-methods
^1680172003598

# Tuples

A tuple is a collection which is {ordered} and {unchangeable}.
^1680174364883

True or False: Tuples allow duplicate values :: True
^1680174364888

Of the four collection data types in python: list, tuple, set, dictionary, these two types does not allow duplicates:: Set and Dictionary
^1680174364892

True or False: You can iterate a tuple through a `for` loop :: True. You can even use `range()` and `len()`
^1680174364896

True or False: Tuples can use `+` and `*` operators:: True. The addition operator adds tuples, the multiplication operator repeats the elements in the tuple.
^1680174364900


