**Centroid-based Clustering** is an *iterative* type of clustering algorithm in which the notion of similarity is derived by the closeness of a data point to the centroid of the clusters.

In these algorithms, the number of clusters must be initially specified. These models run iteratively to find the local optima.

# Examples

[[K-Means Clustering Algorithm]]
[[K-Medoids Clustering Algorithm]]
[[CLARANS]]